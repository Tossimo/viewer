if (images)
    -for (var i=images.length-1; i >= 0; i--) {
        .item
            .holder
                a.glyphicon.glyphicon-trash.imageDel(href="#", data-id="#{images[i]._id}")
                img(src="#{images[i].url}", alt="#{images[i].name}")
    -}
if (image)
    .item
        .holder
            a.glyphicon.glyphicon-trash.imageDel(href="#", data-id="#{image._id}")
            img(src="#{image.url}", alt="#{image.name}")
                                                      